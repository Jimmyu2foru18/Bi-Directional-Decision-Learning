# 03. Mathematical Model

The mathematical foundation of the **Neutral-Centered Bidirectional Decision-Learning Framework (NCBDLF)**.

The framework models decision-making as movement within a continuous latent space centered around a neutral equilibrium point.

The main mathematical components are:

- Neutral-centered manifold representation.
- Distance measurement from neutrality.
- Bidirectional activation dynamics.
- Dual-force weight updates.
- Stability and convergence analysis.

---

# 1. Decision Manifold and Metric Space

Let:

```
𝓜 ⊂ ℝᵈ
```

represent a continuous latent manifold containing all possible decision states.

The neutral reference point is defined as the origin:

```
𝓝₀ = 0
```

where every decision state can be evaluated relative to this neutral anchor.

For any state vector:

```
x ∈ 𝓜
```

the distance from neutrality is measured using a weighted Minkowski metric:

```
Dₙ(x) = ( Σ |xᵢ|ᵖ )¹⁄ᵖ
```

where:

- `p = 2` produces Euclidean stabilization.
- `p → 1` creates a sparse axis-aligned relaxation field.

This metric allows the system to measure how far a decision state has moved from equilibrium.

---

To maintain balanced positive and negative decision movement, the framework introduces the:

**Neutral-Centered Hyperbolic Scaler (NCHS)**

The activation function maintains:

- Point symmetry around zero.
- Bounded activation values.
- Smooth gradient behavior.

The activation function is:

```
NCHS(x) = tanh(αx / 2)
```

where:

```
α > 0
```

represents the elasticity coefficient controlling decision sensitivity.

The derivative used during optimization is:

```
NCHS'(x) = α/2 (1 - NCHS(x)²)
```

This keeps gradient density concentrated near the neutral point.

---

Let:

```
Wₜ ∈ ℝᵐˣᵈ
```

represent the decision projection tensor at time step `t`.

Given an input vector:

```
xₜ
```

the framework generates a target decision vector:

```
yₜ ∈ [-1,1]ᵐ
```

The weight update follows a dual-force optimization process:

```
ΔWₜ = η [ Attractive Force - Neutral Return Force ]
```

The two forces are:

## Attractive Force

Moves the system toward desired outcomes.

```
Eₜxₜᵀ
```

where:

```
Eₜ = yₜ - NCHS(Wₜxₜ)
```

represents the tracking error.

---

## Neutral Return Force

Pulls unstable weights back toward the neutral state.

```
λ sign(Wₜ) ⊙ (Wₜ²)
```

where:

- `λ` = neutral restoration coefficient.
- `⊙` = element-wise multiplication.

The complete update rule becomes:

```
ΔWₜ =
η[
Eₜxₜᵀ
-
λ sign(Wₜ) ⊙ Wₜ²
]
```

This creates a balance between:

- Learning new information.
- Preventing uncontrolled weight growth.

---

## Theorem

Given:

- A bounded input sequence:

```
||xₜ|| ≤ Xₘₐₓ
```

- A bounded target mapping:

```
yₜ ∈ [-1,1]
```

the weight tensor:

```
Wₜ
```

converges toward a stable compact region around:

```
𝓝₀
```

---

Define the energy function:

```
V(Wₜ) = 1/2 ||Wₜ||²F
```

or:

```
V(Wₜ) =
1/2 Tr(WₜᵀWₜ)
```

This represents the total system deviation from neutrality.

For stability:

```
ΔV < 0
```

must occur whenever the system moves outside the stable equilibrium region.

The change in energy is:

```
ΔV =
Tr(WₜᵀΔWₜ)
+
1/2 ||ΔWₜ||²F
```

Substituting the update rule:

```
ΔV =
η Tr(WₜᵀEₜxₜᵀ)
-
ηλ Σ |Wᵢⱼ|³
+
O(η²)
```

---

The neutral return term:

```
-ηλ|Wᵢⱼ|³
```

grows faster than the attractive learning force.

Therefore, there exists a bounded region:

```
||W||F ≤ Ω
```

where:

```
ΔV < 0
```

outside the equilibrium boundary.

By Lyapunov stability principles, the system contracts toward a stable neutral-centered state.

---

The mathematical model establishes NCBDLF as a balanced optimization system based on:

- A neutral-centered decision manifold.
- Symmetric activation behavior.
- Dual-force learning dynamics.
- Stability through restorative weight attraction.

The framework allows continuous learning while maintaining controlled movement around the neutral equilibrium state:

```
𝓝₀ = 0
```
